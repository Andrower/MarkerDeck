name: Build MarkerDeck release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:
    inputs:
      version:
        description: Version without the v prefix
        required: true
        default: "1.7.0"
      publish_release:
        description: Upload packages to a GitHub Release
        type: boolean
        default: false

permissions:
  contents: write

jobs:
  prepare:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.release.outputs.version }}
      tag: ${{ steps.release.outputs.tag }}
      publish: ${{ steps.release.outputs.publish }}
    steps:
      - id: release
        name: Resolve release version
        shell: bash
        env:
          INPUT_VERSION: ${{ inputs.version }}
          INPUT_PUBLISH: ${{ inputs.publish_release }}
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            version="${GITHUB_REF_NAME#v}"
            tag="$GITHUB_REF_NAME"
            publish="true"
          else
            version="$INPUT_VERSION"
            tag="v$INPUT_VERSION"
            publish="$INPUT_PUBLISH"
          fi
          echo "version=$version" >> "$GITHUB_OUTPUT"
          echo "tag=$tag" >> "$GITHUB_OUTPUT"
          echo "publish=$publish" >> "$GITHUB_OUTPUT"

  macos:
    needs: prepare
    runs-on: macos-15-intel
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24.14.0

      - name: Install Node runtime dependencies
        run: npm ci --omit=dev

      - name: Prepare Node.js arm64 runtime
        shell: bash
        run: |
          curl --fail --location --retry 3 \
            --output "$RUNNER_TEMP/node-arm64.tar.gz" \
            "https://nodejs.org/dist/v24.14.0/node-v24.14.0-darwin-arm64.tar.gz"
          tar -xzf "$RUNNER_TEMP/node-arm64.tar.gz" -C "$RUNNER_TEMP"
          mkdir -p runtime/macos/node-macos
          cp "$RUNNER_TEMP/node-v24.14.0-darwin-arm64/bin/node" runtime/macos/node-macos/node
          chmod +x runtime/macos/node-macos/node

      - name: Package macOS arm64
        run: ./scripts/package-macos.sh "${{ needs.prepare.outputs.version }}"

      - uses: actions/upload-artifact@v4
        with:
          name: markerdeck-macos-arm64-${{ needs.prepare.outputs.version }}
          path: artifacts/markerdeck-macos-arm64-v${{ needs.prepare.outputs.version }}.zip
          if-no-files-found: error

  windows:
    needs: prepare
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24.14.0

      - uses: pnpm/action-setup@v4
        with:
          version: 11.9.0
          run_install: false

      - name: Install Node runtime dependencies
        run: npm ci --omit=dev

      - name: Prepare Windows runtimes
        shell: pwsh
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          New-Item -ItemType Directory -Force "runtime\windows\node-windows" | Out-Null
          Copy-Item -Path (Get-Command node).Source -Destination "runtime\windows\node-windows\node.exe" -Force
          .\platform\windows\prepare-ffmpeg.ps1

      - name: Package Windows ZIP
        shell: pwsh
        run: .\scripts\package-windows.ps1 -Version "${{ needs.prepare.outputs.version }}"

      - name: Install desktop dependencies
        working-directory: desktop/electron
        run: pnpm install --frozen-lockfile

      - name: Set desktop package version
        working-directory: desktop/electron
        shell: pwsh
        run: |
          $package = Get-Content "package.json" -Raw | ConvertFrom-Json
          $package.version = "${{ needs.prepare.outputs.version }}"
          $package | ConvertTo-Json -Depth 20 | Set-Content "package.json" -Encoding utf8

      - name: Build desktop client ZIP
        working-directory: desktop/electron
        run: pnpm exec electron-builder --win zip --x64

      - name: Verify desktop client ZIP
        shell: pwsh
        run: |
          $archive = Get-ChildItem "desktop/electron/dist/MarkerDeck.Client.*.zip" | Select-Object -First 1
          if (-not $archive) {
            throw "Desktop client ZIP was not created."
          }

          $verifyRoot = Join-Path $env:RUNNER_TEMP "markerdeck-desktop-verify"
          Expand-Archive -Path $archive.FullName -DestinationPath $verifyRoot -Force

          $requiredFiles = @(
            "MarkerDeck.exe",
            "ffmpeg.dll",
            "resources/server/node-windows/node.exe",
            "resources/server/ffmpeg-windows/ffmpeg.exe",
            "resources/server/node_modules/bonjour-service/package.json",
            "resources/server/node_modules/multicast-dns/index.js"
          )
          foreach ($relativePath in $requiredFiles) {
            $fullPath = Join-Path $verifyRoot $relativePath
            if (-not (Test-Path $fullPath -PathType Leaf)) {
              throw "Desktop client ZIP is missing: $relativePath"
            }
          }

      - uses: actions/upload-artifact@v4
        with:
          name: markerdeck-windows-x64-${{ needs.prepare.outputs.version }}
          path: |
            artifacts/markerdeck-windows-x64-v${{ needs.prepare.outputs.version }}.zip
            desktop/electron/dist/MarkerDeck.Client.*.zip
          if-no-files-found: error

  android:
    needs: prepare
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: gradle

      - uses: android-actions/setup-android@v3

      - name: Install Android SDK platform
        run: sdkmanager "platforms;android-36.1" "build-tools;36.1.0"

      - name: Restore Android release keystore
        env:
          RELEASE_KEYSTORE_BASE64: ${{ secrets.ANDROID_RELEASE_KEYSTORE_BASE64 }}
        shell: bash
        run: |
          if [[ -z "$RELEASE_KEYSTORE_BASE64" ]]; then
            echo "ANDROID_RELEASE_KEYSTORE_BASE64 is not configured" >&2
            exit 1
          fi
          keystore_path="$RUNNER_TEMP/markerdeck-release.jks"
          printf '%s' "$RELEASE_KEYSTORE_BASE64" | base64 --decode > "$keystore_path"
          chmod 600 "$keystore_path"
          echo "ANDROID_RELEASE_KEYSTORE_PATH=$keystore_path" >> "$GITHUB_ENV"

      - name: Build Android release APK
        working-directory: android
        env:
          ANDROID_RELEASE_STORE_PASSWORD: ${{ secrets.ANDROID_RELEASE_STORE_PASSWORD }}
          ANDROID_RELEASE_KEY_ALIAS: ${{ secrets.ANDROID_RELEASE_KEY_ALIAS }}
          ANDROID_RELEASE_KEY_PASSWORD: ${{ secrets.ANDROID_RELEASE_KEY_PASSWORD }}
        run: |
          chmod +x gradlew
          ./gradlew testDebugUnitTest lintRelease assembleRelease

      - name: Verify Android release APK
        env:
          EXPECTED_RELEASE_CERT_SHA256: ${{ vars.ANDROID_RELEASE_CERT_SHA256 }}
          RELEASE_VERSION: ${{ needs.prepare.outputs.version }}
        shell: bash
        run: |
          apk_path="android/app/build/outputs/apk/release/app-release.apk"
          apksigner_path="$ANDROID_HOME/build-tools/36.1.0/apksigner"
          aapt_path="$ANDROID_HOME/build-tools/36.1.0/aapt"
          "$apksigner_path" verify --verbose --print-certs "$apk_path"

          cert_sha256="$({ "$apksigner_path" verify --print-certs "$apk_path"; } \
            | awk -F': ' '/Signer #1 certificate SHA-256 digest/ { print $2; exit }' \
            | tr -d ':[:space:]' \
            | tr '[:lower:]' '[:upper:]')"
          expected_sha256="$(printf '%s' "$EXPECTED_RELEASE_CERT_SHA256" \
            | tr -d ':[:space:]' \
            | tr '[:lower:]' '[:upper:]')"

          if [[ -z "$cert_sha256" ]]; then
            echo "Unable to read the APK signing certificate SHA-256" >&2
            exit 1
          fi
          if [[ -z "$expected_sha256" && "$GITHUB_REF" == refs/tags/v* ]]; then
            echo "ANDROID_RELEASE_CERT_SHA256 must be pinned before a tag release" >&2
            exit 1
          fi
          if [[ -n "$expected_sha256" && "$cert_sha256" != "$expected_sha256" ]]; then
            echo "Android release certificate does not match the pinned SHA-256" >&2
            exit 1
          fi

          package_line="$("$aapt_path" dump badging "$apk_path" | head -n 1)"
          if [[ "$package_line" != *"versionName='$RELEASE_VERSION'"* ]]; then
            echo "Android versionName does not match release version $RELEASE_VERSION" >&2
            exit 1
          fi
          {
            echo "### Android release signing"
            echo
            echo "Certificate SHA-256: \`$cert_sha256\`"
            echo "Version: \`$RELEASE_VERSION\`"
          } >> "$GITHUB_STEP_SUMMARY"

      - name: Prepare Android release asset
        run: |
          mkdir -p artifacts
          cp android/app/build/outputs/apk/release/app-release.apk \
            "artifacts/markerdeck-android-v${{ needs.prepare.outputs.version }}.apk"

      - uses: actions/upload-artifact@v4
        with:
          name: markerdeck-android-${{ needs.prepare.outputs.version }}
          path: artifacts/markerdeck-android-v${{ needs.prepare.outputs.version }}.apk
          if-no-files-found: error

      - name: Remove Android release keystore
        if: always()
        shell: bash
        run: |
          keystore_path="$RUNNER_TEMP/markerdeck-release.jks"
          if [[ -f "$keystore_path" ]]; then
            rm -f -- "$keystore_path"
          fi

  release:
    needs: [prepare, macos, windows, android]
    if: needs.prepare.outputs.publish == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts
          merge-multiple: true

      - name: Create or update GitHub Release
        env:
          GH_TOKEN: ${{ github.token }}
          GH_REPO: ${{ github.repository }}
          RELEASE_TAG: ${{ needs.prepare.outputs.tag }}
          RELEASE_VERSION: ${{ needs.prepare.outputs.version }}
        shell: bash
        run: |
          if ! gh release view "$RELEASE_TAG" >/dev/null 2>&1; then
            gh release create "$RELEASE_TAG" \
              --target "$GITHUB_SHA" \
              --title "MarkerDeck v$RELEASE_VERSION" \
              --generate-notes
          fi
          while IFS= read -r -d '' asset; do
            gh release upload "$RELEASE_TAG" "$asset" --clobber
          done < <(find artifacts -type f \( -name "*.zip" -o -name "*.apk" \) -print0)
